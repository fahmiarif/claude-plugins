import React from 'react';

import { LoginScreen } from '@/screens/Auth/LoginScreen';

// Route file stays thin — all logic lives in the screen component.
export default function Login() {
  return <LoginScreen />;
}
